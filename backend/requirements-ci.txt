# Lightweight CI-only requirements — no GCP SDK (avoids protobuf conflicts).
# Tests that run against this set must not import google-cloud-* at module level.
fastapi==0.115.6
httpx==0.28.1
pydantic==2.10.3
pydantic-settings==2.6.1
pyyaml==6.0.2
msal==1.31.0
python-json-logger==3.2.1
python-dotenv==1.0.1
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
respx==0.21.1
